jenkins:
  clouds:
    - kubernetes:
        name: "kubernetes"
        templates:
          - workspaceVolume:
              genericEphemeralVolume:
                storageClass: my-storageclass
                accessModes: ReadWriteMany
                requestSize: 10Gi
