jenkins:
  clouds:
    - kubernetes:
        name: "kubernetes"
        templates:
          - envVars:
            - secretEnvVar:
                key: key
                secretName: secretName
                secretKey: secretKey
