jenkins:
  clouds:
    - kubernetes:
        name: "kubernetes"
        templates:
          - workspaceVolume:
              persistentVolumeClaimWorkspaceVolume:
                claimName: "my-claim"
